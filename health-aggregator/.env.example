# SQLite (desenvolvimento local)
DATABASE_URL=sqlite:///./data/aggregator.db

# PostgreSQL (produção)
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/health_aggregator

DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10