# AARs HBand / Veepoo

Vendidos a partir de [HBandSDK/Android_Ble_SDK](https://github.com/HBandSDK/Android_Ble_SDK).

Necessários para o handshake (`confirmDevicePwd` → `syncPersonInfo` → `startDetectHeart`).
Conectar só via GATT genérico **não** lê FC nestas pulseiras.
