# Sprint A — código movido

O cliente HTTP vive em `../client` (módulo Gradle).
O BLE (simulador + gancho HBand) vive em `../app/src/main/java/com/healthtech/companion/ble/`.

Este diretório não contém mais Kotlin duplicado.
