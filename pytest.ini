[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    critical: caminho operacional (ingest, alertas, connections, auth)
    research: laboratório (TCN, BMO, hemodinâmica, caos)
